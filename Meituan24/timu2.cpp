#include <iostream>
#include <algorithm>
#include <vector>
#include <map>
#include <unordered_map>
#include <string>
#include <set>
using namespace std;

int main() {
    int T;
    while (cin >> T) {
        int n, k, x;
        cin >> n >> k >> x;
        vector<int> nums(n);
        for (int i = 0; i < n; i++) {
            cin >> nums[i];
        }

        int minCost = INT_MAX;
        
    }
    return 0;
}